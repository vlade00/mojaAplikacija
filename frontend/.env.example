# Kopiraj u .env za lokal (opciono). Za Netlify/Vercel ovo postavi u panelu (Build environment variables).
# Pun URL backend API-ja, sa /api na kraju (ili sam host — automatski se dodaje /api).
REACT_APP_API_URL=http://localhost:3000/api
